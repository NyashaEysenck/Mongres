[package]
name = "mongo-pg-catalog"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
mongo-pg-common = { path = "../common" }
mongo-pg-schema-discovery = { path = "../schema-discovery" }

[lints]
workspace = true
