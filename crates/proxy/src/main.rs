//! `PostgreSQL` wire-protocol proxy entry point.

fn main() {
    eprintln!(
        "mongo-pg-proxy is scaffolded; PostgreSQL wire-protocol serving will be implemented in the protocol milestone."
    );
}
